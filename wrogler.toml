name = "apex-welcome-bot"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables
[vars]
# TELEGRAM_BOT_TOKEN will be set in Cloudflare Dashboard

# Triggers configuration
[triggers]
crons = []

# Environment variables for different environments (optional)
[env.production]
vars = { ENVIRONMENT = "production" }
